//function oper() {
    let width;
    let height;
    let area;
    width=parseInt(prompt("width: "));
    height=parseInt(prompt("height "));
    area=width*height;
    document.write("area: "+area);
//}